'''
Scheduler
Used for the controling the music event on time

'''
import schedule
import time

class InstScheduler():
    def __init__():
        s = sched.scheduler(time.time, time.sleep)
        Inst_dict = {}

    def AddInst(self, Inst_name, Instcontrol):
        '''
        Automatically overwrite
        '''
        Inst_dict[Inst_name] = Instcontrol

    def AddEvent(self, Inst_name, delay_beat):

    def Eventpipeline():


    def __getitem__(self, Inst_name):
        return Inst_dict[Inst_name]
